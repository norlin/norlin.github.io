/* global app */
(function ($, app) {
	var Table = app.createModule({
			name: 'table',
			template: 'table'
		});

	Table.prototype.ready = function () {
		//var module = this;

		//module usage
	};
} (jQuery, app));
/* global -app */