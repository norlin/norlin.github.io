/* global app */
(function ($, app) {
	var Footer = app.createModule({
			name: 'footer',
			template: 'footer'
		});

	Footer.prototype.ready = function () {
		//var module = this;

		//module usage
	};
} (jQuery, app));
/* global -app */